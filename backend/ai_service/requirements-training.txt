# ── PyTorch (CPU) ────────────────────────────────────────────────────────────
# torch/torchvision are NOT installed from this file — scripts/setup_ml_env.sh
# installs the CPU wheels from the PyTorch index (the current env has
# torch==2.13.0+cpu / torchvision==0.28.0+cpu):
#
#   pip install torch==2.13.0+cpu torchvision==0.28.0+cpu \
#       --index-url https://download.pytorch.org/whl/cpu
#
# Kaggle/Colab alternative (GPU wheels are preinstalled there; pin only to
# match local versions, using the extra-index form):
#
#   pip install torch==2.13.0 torchvision==0.28.0 \
#       --extra-index-url https://download.pytorch.org/whl/cpu
#
# faiss-cpu (below) backs the matching_embeddings notebook FAISS index.
tensorflow==2.20.0
tensorflow-text==2.20.1
keras==3.15.1
tf2onnx==1.17.0
onnxruntime==1.28.0
faiss-cpu==1.15.0
datasets==5.0.1
pandas==2.3.3
numpy==2.5.1
scikit-learn==1.9.0
matplotlib==3.11.1
mlflow==3.15.1
h5py==3.16.0
protobuf==6.33.6
ipykernel==7.3.0
ipywidgets==8.1.8
jupyterlab==4.6.2
notebook==7.6.1
einops==0.8.2
